export class DefaultTimeoutConstants {
    public static readonly Default = 40 * 1000;
}

export enum TimeoutConstants {
    MaxTimeout = 8000,
    DefaultTimeout = 2000,
    OpenWsWaitTimeout = 10000,
}